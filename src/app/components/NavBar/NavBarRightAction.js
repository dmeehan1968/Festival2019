import React from 'react'
import styled from 'styled-components'

export const NavBarRightAction = (props) => {
  return (
    <div id="nav-bar-right-action" {...props} />
  )
}

export default styled(NavBarRightAction)`
  justify-self: end;
`
