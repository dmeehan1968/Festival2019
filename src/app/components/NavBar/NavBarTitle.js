import React from 'react'
import styled from 'styled-components'

export const NavBarTitle = props => <div {...props} />

export default styled(NavBarTitle)`
  justify-self: center;
  text-align: center;
`
