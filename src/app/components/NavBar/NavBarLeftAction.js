import React from 'react'
import styled from 'styled-components'

export const NavBarLeftAction = (props) => {
  return (
    <div id="nav-bar-left-action" {...props} />
  )
}

export default styled(NavBarLeftAction)``
