
export function get(req, res, next) {
  const db = req.app.get('db')
  return db.models.tags.scope('regions')
    .findAll()
    .then(regions => regions.map(region => region.toJSON()))
    .then(regions => {
      res.json({ success: true, regions })
    })
    .catch(next)
}
