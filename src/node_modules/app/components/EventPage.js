import React from 'react'
import { connect } from 'react-redux'
import EventList from 'app/components/EventList'
import Event from 'app/components/Event'
import { Helmet } from 'react-helmet'
import EventListFilterPanel from 'app/components/EventListFilterPanel'

import styles from './EventPage.less'
import stringifyClassnames from 'app/helpers/stringifyClassnames'

const EventPage = ({ event }) => {
  return (
  <div className={event ? styles.withEvent : styles.withoutEvent}>
    <Helmet>
      <title>Events</title>
    </Helmet>
    <EventListFilterPanel className={styles.filters} />
    <EventList className={styles.eventlist} />
    {event ? <Event /> : null }
  </div>
)}

const mapStateToProps = state => ({
  event: state.event
})

const mapDispatchToProps = dispatch => ({
  // setEvents: events => dispatch(setEvents(events))
})


export default connect(mapStateToProps, mapDispatchToProps)(EventPage)
