const initialState = null

const event = (state = initialState, action) => {
  switch (action.type) {
    case 'SET_EVENT': {
      return action.event
    }
    default: {
      return state
    }
  }
}

export default event
