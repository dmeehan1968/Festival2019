import { combineReducers } from 'redux'
import event from './event'
import events from './events'
import dates from './dates'
import filters from './filters'

export default combineReducers({ event, events, dates, filters })
