import { combineReducers } from 'redux'
import event from './event'
import events from './events'
import dates from './dates'
import filter from './filter'

export default combineReducers({ event, events, dates, filter })
