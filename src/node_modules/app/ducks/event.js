import { createActions, handleActions } from 'redux-actions'

const defaultState = []

export const { setEvent } = createActions({
  SET_EVENT: event => ({ event })
})

export default handleActions({
  [setEvent]: (state, { payload: { event }}) => event
}, defaultState)
